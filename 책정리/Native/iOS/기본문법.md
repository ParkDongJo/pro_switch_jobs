var x: String
{선언자}{변수명}: {타입}

let <- 상수 선언자
var <- 변수 선언자

String
Float
Double
Bool

Array
let food: [String] = ["egg", "milk"]

Set
let musics: Set\<String\> = ["Rock", "Jazz"]
- 중복 제거
- 순서 없음
- 교집합, 합집합, 부분집합

Dirtionary
let dict = ["name": "dongjo"]
- Key: Value 를 가진 구조



# 조건문

```
if 참/거짓 {

} else {

}
```


```swift
guard 참/거짓 {
	// 조건문이 거짓일 때
}

guard let letMessage = message else { 
	return '' 
}

```


# 반복문

ForEach
```swift

let data = ["lee", "park"]
forEach(data, id: \.self) { item in
	print(item);						   
}


forEach(데이터, 식별값) { item in
	// 코드
}
```

for in/of
while


# 함수

```swift
fucn 함수이름(매개변수 이름: 값) -> 반환타입 {
	// 코드
	return 5
}

```



# 구조체
```swift
struct 구조체이름 {
	프로퍼티 (속성)
	메소드 (동작)	  
}
```


# 클래스
클래스 vs. 구조체
\= 주소 vs. 값
\= Google 스프레드시트 vs. MS 엑셀
```swift
class 클레스이름 {
	프로퍼티 (속성)
	init (생성자)
	메소드 (동작)	  
}
```