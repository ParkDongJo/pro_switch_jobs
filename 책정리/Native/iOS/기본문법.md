var x: String
{선언자}{변수명}: {타입}

let <- 상수 선언자
var <- 변수 선언자

String
Float
Double
Bool

Array
let food: [String] = ["egg", "milk"]

Set
let musics: Set\<String\> = ["Rock", "Jazz"]
- 중복 제거
- 순서 없음
- 교집합, 합집합, 부분집합

Dirtionary
let dict = ["name": "dongjo"]
- Key: Value 를 가진 구조



# 조건문
- if
- gaurd

```swift
if 참/거짓 {

} else {

}
```


```swift
guard 참/거짓 {
	// 조건문이 거짓일 때
}

guard let letMessage = message else { 
	return '' 
}

```


# Switch

```
```



# 반복문

ForEach
```swift

let data = ["lee", "park"]
forEach(data, id: \.self) { item in
	print(item);						   
}


forEach(데이터, 식별값) { item in
	// 코드
}
```

for in/of
while


# 함수

```swift
fucn 함수이름(매개변수 이름: 타입) -> 반환타입 {
	// 코드
	return 5
}

```



# 구조체
```swift
struct 구조체이름 {
	프로퍼티 (속성)
	메소드 (동작)	  
}
```


# 클래스
클래스 vs. 구조체
\= 주소 vs. 값
\= Google 스프레드시트 vs. MS 엑셀
```swift
class 클레스이름 {
	프로퍼티 (속성)
	init (생성자)
	메소드 (동작)	  
}
```


# 열거형
- 선택지를 만들어 주는것
```json
enum Direction {
	case north
	case east
	case west
	case south
}

enum Direction: String {
    case north = "북"
    case west = "서"
    case east = "동"
    case south = "남"
}

```



# 옵셔널
- 있을 수도 있고 없을 수도 있는 값
```swift
let name: String? = nil
```


### 옵셔널 바인딩
```swift 
let petName: String? = "hany"
if let name = patName {
	// 코드
}
```


### 옵셔널 체이닝
```swift
struct Pet {
	let age: Int?
	let level: Int?
}

let pat: Pet?

if let name = pet?.age?.level {
	print(name)
}

```