# 이벤트

props 를 넘길 때는 handle{이름}
props 로 받을 때는 on{이름}

이벤트 핸들러를 생성하는 함수는
handle{이름} 이 아니라 create{이름}Handler 으로 바꾸자


# 커스텀 이벤트
on{커스텀한 이름} 형태로  커스텀 이벤트를 만들수 있다.
하지만, 기존에 있는 on{이름} 이 있다면, 그대로 사용하는 것을 권장함


# 이벤트 전파
하위 자식에서 이벤트가 발생 시 상위 부모로 이벤트 전파 될때에 대한 내용을 React 문서에서도 설명

자식 컴포넌트에서 이벤트 전파를 막도록 설명

> 부여된 JSX 태그 내에서만 실행되는 `onScroll`을 제외한 React 내의 모든 이벤트는 전파됩니다.


# 이벤트 캡처
onClickCapture 로 이벤트 캡처를 의도적으로 구현할 수 있다.

```tsx
<div onClickCapture={() => { /* this runs first */ }}>  
  <button onClick={e => e.stopPropagation()} />  
  <button onClick={e => e.stopPropagation()} />  
</div>
```

1. 아래로 전달되면서 만나는 모든 `onClickCapture` 핸들러를 호출합니다.
2. 클릭된 요소의 `onClick` 핸들러를 실행합니다.
3. 위로 전달되면서 만나는 모든 `onClick` 핸들러를 호출합니다.


# 해당 내용의 키워드
- 네이밍에 대한 가이드라인
- [`e.stopPropagation()`](https://developer.mozilla.org/docs/Web/API/Event/stopPropagation)은 이벤트 핸들러가 상위 태그에서 실행되지 않도록 멈춥니다.
- [`e.preventDefault()`](https://developer.mozilla.org/docs/Web/API/Event/preventDefault) 는 기본 브라우저 동작을 가진 일부 이벤트가 해당 기본 동작을 실행하지 않도록 방지합니다.


# 이벤트 위임
최상위 루트에만 이벤트 등록
- 메모리 최적화
- 구조 간결화

해당 개념을 기반으로 한다면,
- ref 를 피하는것이 좋은 이유


# ComponentProps
React 내부에서 ComponentProps 인터페이스를 제공해주고 있고, ComponentProps<'button'>, ComponentProps<'span'> 등등으로 제네릭을 활용하여 쉽게 Props 를 정의할 수 있음

https://velog.io/@kmh060020/%EC%9C%A0%EC%9A%A9%ED%95%9C-ComponentProps-%EC%82%AC%EC%9A%A9%ED%95%98%EA%B8%B0


# nuqs
querystring 을 react 에서 내부적으로 관리할 수 있도록 돕는 라이브러리
https://github.com/47ng/nuqs