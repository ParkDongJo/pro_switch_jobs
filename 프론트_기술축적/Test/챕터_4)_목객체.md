# 목 객체

### 스텁 (stub)

## 모듈 A에 의존중인 대상을 테스트 할 때, `'A 에서 B 값을 반환받았을 때는 C같이 작동해야한다'` 라는 테스트에 스텁을 사용한다.

### 스파이 (spy)

테스트 대상의 외부 출력

- 테스트 대상의 인수에 함수가 있을 때 유용하게 활용할 수 있다.

- 목객체

# 라이브러리 대체하기

모듈을 불러오는 방법에는 2가지 방법이 있다.

- ESM (ES Module) : import 로 외부 노출
- CJS (CommonJS Modules) : require 로 외부 노출
